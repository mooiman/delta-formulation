# Logging = "matrix"

[Domain]
    Lx = 10000.0                                  # Domain length [m]
    depth = 10.0                                  # bathymetry below reference level [m]

[Time]
    tstart = 0.0                                 # Start time [s]
    tstop = 360000.0                                # End time [s]
        
[Initial]
    shape = "Constant"                                 # "Constant", "Envelope"
    
[Boundary]
    bc_type = ["dirichlet", "dirichlet"]          # Type "dirichlet", "borsboom" 
    bc_signal = ["constant", "constant"]          # Type "constant", "sine" 
    bc_vals = [1.0, 10.0]                          # The inflow discharge
    treg = 600.0                                  # Regularization time on boundary

[Physics]
    g = 0.0                              # acceleration due to gravity            
    momentum_viscosity = true                     # inclide viscosity
    viscosity = 300.0
    
[Numerics]
    dt = 10.0                                    # Time step size [s], if dt == 0: then stationary problem
    dx = 1000.0                                     # Grid size [m]
    c_psi = 4.0                                  
    theta = 0.501                                # Implicitness factor (0.5 <= theta <= 1.0)
    alpha = 0.125                                # Linear (spatial) interpolation coefficient
    iter_max = 50                               # Maximum number of nonlinear iterations
    eps_newton = 1.0e-12
    eps_bicgstab = 1.0e-12

[Output]
    dt_his = 1.0   # [s]
    dt_map = 1.0   # [s] 
